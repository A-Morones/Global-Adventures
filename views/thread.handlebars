<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{thread.title}}</title>
    <link rel="stylesheet" href="/styles/main.css"> <!-- Link to your stylesheet -->
</head>

    <main>
        <article>
            <h2>{{thread.title}}</h2>
            <p>{{thread.content}}</p>
        </article>

        <section>
            <h3>Comments</h3>
            {{#if comments.length}}
                <ul>
                    {{#each comments}}
                        <li>
                            <strong>{{this.user.username}}:</strong>
                            <p>{{this.comment}}</p>
                        </li>
                    {{/each}}
                </ul>
            {{else}}
                <p>No comments yet.</p>
            {{/if}}
        </section>

        {{#if user}}
            <section>
                <h3>Add a Comment</h3>
                <form action="/threads/comment/{{thread.id}}" method="POST">
                    <textarea name="comment" placeholder="Add your comment here" required></textarea>
                    <button type="submit">Submit Comment</button>
                </form>
            </section>
        {{else}}
            <section>
                <p><a href="/public/js/login.js">Login</a> to leave a comment.</p>
            </section>
        {{/if}}
    </main>

    <footer>
        <p>&copy; 2024 My Forum</p>
    </footer>
</body>
</html>
