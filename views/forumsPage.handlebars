<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <link rel="stylesheet" href="/styles/main.css"> <!-- Link to your stylesheet -->
</head>
<body>
    <header>
        <h1>{{title}}</h1>
        <nav>
            <ul>
                <li><a href="/">Home</a></li>
                {{#if user}}
                    <li><a href="/threads/new">Create New Thread</a></li>
                    <li><a href="/users/logout">Logout</a></li>
                {{else}}
                    <li><a href="/users/login">Login</a></li>
                    <li><a href="/users/register">Register</a></li>
                {{/if}}
            </ul>
        </nav>
    </header>

    <main>
        <h2>Forum Threads</h2>
        
        {{#if threads.length}}
            <ul>
                {{#each threads}}
                    <li>
                        <h3><a href="/threads/{{this.id}}">{{this.title}}</a></h3>
                        <p>{{this.preview}}</p>
                        <small>Posted by {{this.user.username}} on {{this.createdAt}}</small>
                    </li>
                {{/each}}
            </ul>
        {{else}}
            <p>No threads available. <a href="/threads/new">Create a new thread</a></p>
        {{/if}}
    </main>

    <footer>
        <p>&copy; 2024 My Forum</p>
    </footer>
</body>
</html>
